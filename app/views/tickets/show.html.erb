<div id="ticket">
	<h2><%= @ticket.title %></h2>
	<%= link_to "Edit Ticket", [:edit, @project, @ticket] %>
	<!-- simple_format converts line breaks entered into text field into html breaks -->
	<%= simple_format(@ticket.description) %>
</div>


